L1
1) prevent "Add variable" when not in "Variables" section
   - done
2) prevent "Remove variable" when not in "Variables" section
   - done
3) add "Browse..." on right click in file_path, in_filename
   and out_filename values
   - done
4) change "Add variable" on right click of [Global] to 
   "Add attribute"
   - done
5) change "Remove variable" on right click of global attribute
   to "Remove attribute"
   - done
6) disable "Remove variable" for xlDateTime
7) disable "Add..." on right click of [[[xl]]]
   - done
8) implement "Add attribute" on right click of [[[Attr]]]
   - done
9) implement "Remove attribute" on right click of [[[Attr]]] key
   - done
10) implement support for CSV files
11) implement support for [Output]
12) disable "Remove item" on right click in [Files] section
    - done

L2
1) browse for plot_path
2) disable editing of [Files] section keys
3) remove "Add ExcludeHours" and "Add linear"
4) disable adding of QC check if QC check already specified for
   variable
5) in [Plots], use Title as plot key
6) trap "Run..." and "Quit..." without save
7) automatically re-plot variable when any QC for that variable
   is changed
8) implement support for [Output] and [Options]

L3
1) move zms from [General] to [Options]
2) prevent "Add variable" when not in "Variables" section
   - done
3) prevent "Add QC check" when not in "Variables" section
   - done
4) add "Browse..." on fight click in [Files] section
   - done
5) add "Add item" on right click in [Files] section
   - done
6) implement plot_path in [Files] section
7) implement "Add option" on right click in [Options] section
8) disable "Add..." and "Remove..." from [Soils] section
   - done
9) disable "Add..." and "Remove..." from [Massman] section
   - done
10) implement "Add RangeCheck" etc in [Variables]
    - done
11) implement "Add MergeSeries" and "AverageSeries" in [Variables]
    - done

Concatenate
1) implement right-click menus
   - done

Climatology
1) implement climatology

CPD
1) implement CPD

L4
1) implement get_model_from_data
   - done
2) implement get_data_from_model
   - done
3) implement right-click context menu
4) implement handleItemChanged
   - done
5) [Files] section
   - "Add item"
     - done
   - "Remove item"
     - done
   - "Browse"
     - done
6) [Global] section
7) [Options] section
   - done
8) [Import] section
9) [Drivers] or [Variables] section
   - "Add variable"
   - "Remove variable"
   - "Add method"
   - "Remove method"
   - "Add QC check"
   - "Remove QC check"
   - "Add MergeSeries"
   - "Remove MergeSeries"
10) automagic population of new variable fields based on contents of
    [Files] section

L5

L6